## What this changes

<!-- What was wrong, and why this fixes it. -->

## Checks

- [ ] `make test` passes
- [ ] `make lint` passes
- [ ] `make arch` passes
- [ ] `make bench` still scores at or above the baseline (if generation changed)
- [ ] New behaviour has a test that fails without this change

## Notes for the reviewer

<!-- Anything surprising you found, or a decision worth questioning. -->
